# API and Socket URLs — leave empty to use Vite proxy (recommended for development).
# The proxy is configured in vite.config.js and avoids CORS issues automatically.
# Uncomment with full URLs only if you need to bypass the proxy:
# VITE_API_URL=http://localhost:5001/api
# VITE_SOCKET_URL=http://localhost:5001
