# Server
PORT=5001
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/cafe_management

# Authentication — generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_256_BIT_SECRET

# CORS — the URL where your frontend is served
CLIENT_URL=http://localhost:5173
